apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-service
  namespace: adaptix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pos-service
  template:
    metadata:
      labels:
        app: pos-service
    spec:
      containers:
        - name: pos-service
          image: adaptix-pos-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: adaptix-config
            - secretRef:
                name: adaptix-secrets
          env:
            - name: SERVICE_NAME
              value: "pos-service"
          livenessProbe:
            httpGet:
              path: /health/
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: pos-service
  namespace: adaptix
spec:
  selector:
    app: pos-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
